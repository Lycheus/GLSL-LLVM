int sub1test(int a)
{
  int x=3, y;
  y = a > x ? (a+1) : (x+1);
  return (y);
}

float BickColor=0.0;
int abc123=0;
float MortarColor=0.0;
float BrickSize=0.0;
float BrickPct=0.0;
float MCposition=0.0;
float LightIntensity=0.0;

uniform vec3 test1;
varying vec2 test2;
varying float test3;

float Fract(float x)
{ return (1.0/x);
}

float shufflevector(float w,float y,float x,float z)
{ return (1.0/x);
}

void main()
{ float a,b,c,d, position;

  a = MCposition / BrickSize;
  position = shufflevector(a,b,c,d);
}
